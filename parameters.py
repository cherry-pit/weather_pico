# Enter wifi information here
WIFI_SSID = "XYZ"
WIFI_PASSWORD = "ABC"
### Change latitude and longitude information to where weather should be reported for
#
lat, long =  XX.XXXX, XX.XXXX
timezone_offset = X # hour offset from GMT timezone | for example -6 for CST